The /home/kali/data directory where this file is found lives on the persistence
volume. Data stored here will survive reboots of the liveCD image.

Create other files/folders like this one by:

1) edit /run/live/persistence/sda1/persistence.conf
2) create files/folders under /run/live/persistence/sda1/path/to/file
